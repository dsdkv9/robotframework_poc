pipeline {
  agent any
  stages {
    stage("build") {
      steps {
        echo 'Jenkins stage .. build'
      }
    }
    stage("test") {
      steps {
        echo 'Jenkins stage .. test'
      }
    }
    stage("release") {
      steps {
        echo 'Jenkins .. release ..'
      }
    }
  }
}
